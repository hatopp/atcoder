#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> x >> a;
    ans = 0;
    if (x - a > 0)
    {
        ans = x - a;
    }

    cout << ans << endl;
    return 0;
}