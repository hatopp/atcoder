#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int n, m, t, a, b;
    cin >> n >> m >> t;

    vector<pair<int, int>> vp(m);
    for (int i = 0; i < m; ++i)
    {
        cin >> a >> b;
        vp[i]=pair(a, b);
    }

int mae=0;
int ato;

    for (int i=0; i<m; i++) {
        if(

    for (int i = 0; i < m; i++)
    {
        cout << vp[i].first << " " << vp[i].second << endl;
    }
    return 0;
}
