#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n;
    string s;
    cin >> a >> b >> c >> d;
    int ans = min({a, b, c, d});
    cout << ans << endl;
    return 0;
}