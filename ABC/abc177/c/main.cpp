#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
	ll n;
	cin >> n;

	ll ans;
	ll mod = 1000000007;
	vector<ll> v(n);
	for (ll i = 0; i < n; i++)
	{
		cin >> v[i];
		ans %= mod;
	}
	ll ans = v[0] * v[n] % wa;
	for (ll i = 0; i < n; i++)
	{
		ans +=
			cout << ans << endl;
		return 0;
	}