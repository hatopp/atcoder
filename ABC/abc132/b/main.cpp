#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;

    vector<int> v(n);
    for (int i = 0; i < n; ++i)
    {
        cin >> v[i];
    }

    for (int i = 1; i < n - 1; ++i)
    {
        min(v[i-1],v[i],)

    }
}
