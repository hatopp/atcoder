#include <bits/stdc++.h>
using namespace std;

int main()
{
    int r,g;
    cin >> r>>g ;
    
    double dans= (g*2)-r;
    int ans = ceil(dans);

    cout << ans << endl;
    return 0;
}
