#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
    ll a, b, c, d, e, f, x;
    cin >> a >> b >> c >> d >> e >> f >> x;

    int t, a;

    for (int i = 0; i < x; i++)
    {
