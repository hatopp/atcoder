#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> n;
    sans = "ABC";
    if (n >= 1000)
    {
        sans = "ABD";
    }

    cout << sans << endl;
    return 0;
}