#include <bits/stdc++.h>
using namespace std;

int main() {
    int n,a;
    cin >> n >> a ;
    int count = 0;
    
    vector<int> card(n);
    for (int i=0; i<n; ++i){
        cin >> card[i];
    }
    sort(card.begin(),card.end());
    
    for (int i=0; i<n; ++i){
            wa = i;


        for (int j=0; j<    