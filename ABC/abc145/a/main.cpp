#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> a;
    // ans = (a * a * c) / (1 * 1 * c);
    cout << a * a << endl;
    return 0;
}