#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)

using namespace		std;
typedef long long	ll;

int	main(void)
{
	int	ans;

	int x, y, z;
	cin >> x >> y >> z;
	// x:goal y:wall z:hammer
	ans = 0;
	// ans = 2 * abs(z - y)) + abs(y) + abs(x);
	// ans = abs(z) + abs(y) + abs(x);

	if (x < 0)
	{
		y = -
	}
}
