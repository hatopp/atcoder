#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;

int main()
{
	string a;

	cin >> a;
	int len = a.size();
	if (len > 1)
	{
		cout << 'a' << endl;
	}
	else if ( a != "a")
	{
		cout << 'a' << endl;
	}
	else
	{
		cout << -1 << endl;
	}
	return 0;
}
