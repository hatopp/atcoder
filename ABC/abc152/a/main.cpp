#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> a >> b;
    sans = "No";
    if (a == b)
    {
        sans = "Yes";
    }

    cout << sans << endl;
    return 0;
}
