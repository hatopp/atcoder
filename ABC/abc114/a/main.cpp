#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> x;
    sans = "NO";
    if (x == 7 || x == 5 || x == 3)
    {
        sans = "YES";
    }
    cout << sans << endl;
    return 0;
}