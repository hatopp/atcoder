#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a, b, c, d, x, y, z, n, count, ans;
    string s, sans;
    cin >> a >> b;
    double dans = (a - b) / a * 100.00;

    cout << dans << endl;
    return 0;
}