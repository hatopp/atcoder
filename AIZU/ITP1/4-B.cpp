#include <bits/stdc++.h>
using namespace std;

int main()
{
    double r;
    cin >> r;
    double p = M_PI;
    cout << setprecision(16) << r * r * p << " " << setprecision(16) << 2 * r * p << endl;
    return 0;
}